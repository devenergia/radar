# =============================================================================
# PROJETO RADAR - Roraima Energia
# Arquivo de exemplo de variaveis de ambiente
# Copie este arquivo para .env e preencha com os valores reais
# NUNCA commite o arquivo .env no repositorio!
# =============================================================================

# -----------------------------------------------------------------------------
# AMBIENTE
# -----------------------------------------------------------------------------
# Valores: development, homologation, production (ou dev, hm, prd)
ENVIRONMENT=development
APP_NAME=radar-api
APP_VERSION=1.0.0

# -----------------------------------------------------------------------------
# SERVIDOR
# -----------------------------------------------------------------------------
HOST=0.0.0.0
PORT=8000
WORKERS=4
DEBUG=true

# =============================================================================
# BANCO DE DADOS ORACLE
# =============================================================================
# O sistema suporta configuracao multi-ambiente com prefixos DEV_, HM_, PRD_
# As variaveis com prefixo tem prioridade sobre as sem prefixo
#
# Ordem de prioridade:
# 1. {ENV}_ORACLE_* (ex: PRD_ORACLE_DSN para producao)
# 2. ORACLE_* (fallback)

# -----------------------------------------------------------------------------
# ORACLE - Desenvolvimento (prefixo DEV_)
# -----------------------------------------------------------------------------
DEV_ORACLE_USER=radar_api
DEV_ORACLE_PASSWORD=
DEV_ORACLE_DSN=localhost:1521/RADARDEV

# -----------------------------------------------------------------------------
# ORACLE - Homologacao (prefixo HM_)
# -----------------------------------------------------------------------------
HM_ORACLE_USER=radar_api
HM_ORACLE_PASSWORD=
HM_ORACLE_DSN=servidor_hm:1521/RADARHM

# -----------------------------------------------------------------------------
# ORACLE - Producao (prefixo PRD_)
# -----------------------------------------------------------------------------
PRD_ORACLE_USER=radar_api
PRD_ORACLE_PASSWORD=
PRD_ORACLE_DSN=servidor_prd:1521/RADARPRD

# -----------------------------------------------------------------------------
# ORACLE - Fallback (usado quando nao ha prefixo de ambiente)
# -----------------------------------------------------------------------------
ORACLE_USER=radar_api
ORACLE_PASSWORD=
ORACLE_DSN=localhost:1521/RADARDB

# -----------------------------------------------------------------------------
# ORACLE - Pool de Conexoes
# -----------------------------------------------------------------------------
ORACLE_POOL_SIZE=20
ORACLE_POOL_MAX_OVERFLOW=10
ORACLE_POOL_TIMEOUT=30
ORACLE_POOL_RECYCLE=1800

# -----------------------------------------------------------------------------
# DBLINKS CONFIGURADOS NO BANCO RADAR
# (Apenas para referencia - os DBLinks sao configurados no Oracle)
# -----------------------------------------------------------------------------
# DBLINK_INSERVICE=INSERVICE_LINK
# DBLINK_INDICADORES=INDICADORES_LINK

# =============================================================================
# SERVICO DE EMAIL (Mailgun)
# =============================================================================
# O sistema suporta configuracao multi-ambiente com prefixos DEV_, HM_, PRD_

# -----------------------------------------------------------------------------
# MAILGUN - Desenvolvimento (prefixo DEV_)
# -----------------------------------------------------------------------------
# Em desenvolvimento, use o sandbox do Mailgun
DEV_MAILGUN_API_KEY=key-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
DEV_MAILGUN_DOMAIN=sandbox.mailgun.org
DEV_MAILGUN_SENDER=noreply@sandbox.mailgun.org
DEV_MAILGUN_SENDER_NAME=RADAR DEV

# -----------------------------------------------------------------------------
# MAILGUN - Homologacao (prefixo HM_)
# -----------------------------------------------------------------------------
HM_MAILGUN_API_KEY=key-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
HM_MAILGUN_DOMAIN=hm.roraimaenergia.com.br
HM_MAILGUN_SENDER=radar-hm@roraimaenergia.com.br
HM_MAILGUN_SENDER_NAME=RADAR HM

# -----------------------------------------------------------------------------
# MAILGUN - Producao (prefixo PRD_)
# -----------------------------------------------------------------------------
PRD_MAILGUN_API_KEY=key-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
PRD_MAILGUN_DOMAIN=roraimaenergia.com.br
PRD_MAILGUN_SENDER=radar@roraimaenergia.com.br
PRD_MAILGUN_SENDER_NAME=RADAR

# -----------------------------------------------------------------------------
# MAILGUN - Fallback
# -----------------------------------------------------------------------------
MAILGUN_API_KEY=
MAILGUN_DOMAIN=
MAILGUN_SENDER=
MAILGUN_SENDER_NAME=RADAR

# -----------------------------------------------------------------------------
# AUTENTICACAO
# -----------------------------------------------------------------------------
# Chave API para autenticacao da ANEEL
# Gere uma chave segura: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=

# -----------------------------------------------------------------------------
# WHITELIST DE IPs (separados por virgula)
# -----------------------------------------------------------------------------
# IPs da ANEEL autorizados a consultar a API
# Use * para permitir todos (apenas em desenvolvimento)
ALLOWED_IPS=*

# -----------------------------------------------------------------------------
# LOGS
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO
LOG_FORMAT=json

# -----------------------------------------------------------------------------
# CACHE
# -----------------------------------------------------------------------------
CACHE_TTL_SECONDS=300
CACHE_STALE_TTL_SECONDS=3600
CACHE_MAX_ITEMS=1000

# -----------------------------------------------------------------------------
# METRICAS E MONITORAMENTO
# -----------------------------------------------------------------------------
METRICS_ENABLED=true

# -----------------------------------------------------------------------------
# CORS (Frontend Vue.js)
# -----------------------------------------------------------------------------
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# -----------------------------------------------------------------------------
# EMAIL PARA INDISPONIBILIDADE (usado nas respostas da API)
# -----------------------------------------------------------------------------
EMAIL_INDISPONIBILIDADE=radar@roraimaenergia.com.br

# -----------------------------------------------------------------------------
# ALERTAS DE ERRO
# -----------------------------------------------------------------------------
# Email que recebera alertas de erros criticos
ALERT_EMAIL=ti.desenvolvimento@eletrororaima.com
